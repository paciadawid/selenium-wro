from selenium.webdriver.common.by import By

from selenium.webdriver.common.by import By

input_search_selector = (By.ID, "search_query_top")
loop_selector = (By.NAME, "submit_search")
add_to_cart_selector = (By.CSS_SELECTOR, ".ajax_add_to_cart_button")
proceed_to_checkout_selector = (By.XPATH, "//*[@title='Proceed to checkout']")
